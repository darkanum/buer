{
  "name": "@onewash/gi-data",
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json",
    "sync": "node scripts/sync.ts",
    "assets:sync": "node scripts/assets-sync.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.6",
    "vitest": "^2"
  }
}
