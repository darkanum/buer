#!/usr/bin/env sh
if git diff --cached --name-only -z -- . ':!.husky' | xargs -0 grep -lE 'ltoken_v2=|ltuid_v2=|cookie_token_v2=' 2>/dev/null; then
  echo "ERRO: possível cookie do HoYoLAB no diff. Aborte e remova o segredo." >&2
  exit 1
fi
